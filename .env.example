# Copy to .env and fill in. NEVER commit the real .env (it is gitignored).
# CloudNova core needs no secrets; these are for later phases (live cloud + AI).

# --- AI agents (Phase 5) ---
ANTHROPIC_API_KEY=

# --- Live cloud scanning (Phase 2): prefer a real AWS credential provider ---
# (env vars, SSO, or an assumed read-only role) over pasting keys here.
AWS_REGION=us-east-1
